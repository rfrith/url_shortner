<%= form_for(@short_url) do |f| %>
  <% if @short_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@short_url.errors.count, "error") %> prohibited this short_url from being saved:</h2>

      <ul>
        <% @short_url.errors.messages.values.each do |message| %>
          <% message.each do  |m| %>
            <li><%= m %></li>
          <% end %>
        <% end %>
      </ul>

    </div>
  <% end %>

  <div class="field">
    <%= f.label "Enter the URL You Would Like To Shorten Here:" %><br>
    <%= f.text_field :long, :size => "80" %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Create Short URL" %>
  </div>
<% end %>
